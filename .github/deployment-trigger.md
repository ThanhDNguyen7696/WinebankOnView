# Deployment trigger

This file records the clean redeployment of the current `main` application after the Supabase authentication and menu administration changes were merged.

Deployment configuration remains supplied through environment variables. Local `.env` files and generated Supabase configuration are excluded from Git.
